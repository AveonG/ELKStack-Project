#!/bin/bash

# Execute the script before running it by chmod +x ./(name of script).sh

# Confirming all apts are updated with the most current update and upgrading all installed packages
apt update -y && apt upgrade -y